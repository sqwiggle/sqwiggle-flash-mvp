package sqwiggle {	import flash.display.Sprite;	import flash.events.NetStatusEvent;	import flash.media.Video;	import flash.media.SoundChannel;	import flash.net.NetConnection;	import flash.net.NetStream;	import flash.display.Shape;	import flash.display.MovieClip;	import flash.events.MouseEvent;	import flash.utils.Timer;		public class Member extends Sprite {		public var connection	:NetConnection;		public var userId		:String;		public var peerId    	:String;		public var audioPeerId  :String;		var stream:NetStream;		var astream:NetStream;		var video :Video;		var audio :Video;				var bar:MovieClip;		var t:MovieClip;				public function Member(userId:String, peerId:String) {			this.userId = userId;			this.peerId = peerId;			video = new Video();			video.width  = 291;			video.height = 218;			addChild(video);			audio = new Video();			audio.width  = 100;			audio.height = 100;			addChild(audio);			bar = new fuckyou();			bar.y = video.height;			bar.visible=false;			addChild(bar);						t = new talk();			t.x = 4;			t.buttonMode=true;			t.useHandCursor=true;			t.y = video.height - 4;			addChild(t);			t.visible=false;						addEventListener(MouseEvent.MOUSE_OVER, mover);			addEventListener(MouseEvent.MOUSE_OUT, mout);		}				public function connectToVideo(videoConnection:NetConnection):void {			trace('Connecting to video of: ' + userId + ', stream: ' + peerId);						stream = new NetStream(videoConnection, peerId);			stream.addEventListener(NetStatusEvent.NET_STATUS, onVideoStatus);			stream.play(this.userId);						video.attachNetStream(stream);		}				public function connectToAudio(audioConnection:NetConnection, peerId:String):void {			trace('Connecting to audio of: ' + userId + ', stream: ' + peerId);						astream = new NetStream(audioConnection, peerId);			astream.receiveVideo(false);			astream.addEventListener(NetStatusEvent.NET_STATUS, onAudioStatus);			astream.play('audio' + this.userId);		}				public function onVideoStatus(e:NetStatusEvent):void {			trace('Video status ' + peerId + ': ' + e.info.code);		}				public function onAudioStatus(e:NetStatusEvent):void {			trace('Audio status ' + peerId + ': ' + e.info.code);		}				public function remove():void {			removeChild(video);			removeChild(audio);			if (stream) stream.close();			if (astream) astream.close();		}				function mover(e:*):void {			bar.visible = true;			t.visible = true;		}				function mout(e:*):void {			bar.visible = false;			t.visible = false;		}	}}